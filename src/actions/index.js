export const addData = (arr) => {
    return {
        type: 'setData',
        payload: arr
    }
}

export const addData1 = (arr) => {
    return {
        type: 'setData1',
        payload: arr
    }
}